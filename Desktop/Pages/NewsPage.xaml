<Page x:Class="Desktop.Pages.NewsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Desktop.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Новости">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="20" Background="#F9F9F9" BorderBrush="#EEE" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="📰 Управление новостями" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Foreground="#333" 
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">


                    <Button x:Name="BtnDelete" 
                            Content="🗑 Удалить выбранные" 
                            Click="BtnDelete_Click" 
                            Background="#D32F2F" 
                            Foreground="White"
                            Padding="15,8"
                            BorderThickness="0"
                            Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <ListView x:Name="NewsList" 
                  Grid.Row="1" 
                  Padding="20" 
                  BorderThickness="0" 
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Margin" Value="0,0,0,15"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Background="White" 
                            BorderBrush="#E0E0E0" 
                            BorderThickness="1" 
                            CornerRadius="8" 
                            Padding="15">
                        <Border.Effect>
                            <DropShadowEffect Color="#000" Opacity="0.05" BlurRadius="5" ShadowDepth="2"/>
                        </Border.Effect>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="1" CornerRadius="6" ClipToBounds="True" Height="80" Width="120">
                                <Image Source="{Binding ImageUrl}" Stretch="UniformToFill"/>
                            </Border>

                            <StackPanel Grid.Column="2" Margin="15,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Title}" 
                                           FontWeight="SemiBold" 
                                           FontSize="16" 
                                           Foreground="#333" 
                                           TextWrapping="Wrap"
                                           Margin="0,0,0,5"/>

                                <TextBlock Text="{Binding Description}" 
                                           FontSize="13" 
                                           Foreground="#666" 
                                           TextWrapping="Wrap" 
                                           MaxHeight="40" 
                                           TextTrimming="WordEllipsis"
                                           Margin="0,0,0,8"/>

                                <TextBlock Text="{Binding PublishDate, StringFormat='{}{0:dd.MM.yyyy HH:mm}'}" 
                                           FontSize="12" 
                                           Foreground="{DynamicResource AccentBrush}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Grid x:Name="LoadingOverlay" Visibility="Collapsed" Background="#80FFFFFF" Grid.RowSpan="2">
            <ProgressBar IsIndeterminate="True" Width="200" Height="20" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</Page>